# USAP Test Suite Requirements

# Core dependencies
PyPDF2>=3.0.0  # For PDF text extraction (OpenAI provider)

# LLM Providers (install the one you plan to use)
anthropic>=0.40.0  # For Claude (native PDF support)
openai>=1.0.0  # For GPT (uses text extraction)
google-generativeai>=0.3.0  # For Gemini (native PDF support)

# Optional: For advanced reporting
pandas>=2.0.0
matplotlib>=3.7.0
